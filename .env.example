# Database (SQLite for Phase 1; use postgresql://... for production)
DATABASE_URL="file:./dev.db"

# OpenAI — for AI interpretation (POST /api/test/ai-analysis)
OPENAI_API_KEY="sk-..."

# Razorpay — for Career Intelligence Test payment (₹499)
RAZORPAY_KEY_ID="your_key"
RAZORPAY_KEY_SECRET="your_secret"

# Admin — for /whats-next/admin/login
ADMIN_EMAIL="admin@mib2.in"
ADMIN_PASSWORD="makeitbeautiful"

# Test account (bypasses payments, sees all reports) is created via: npx tsx scripts/create-test-user.ts
# Login: nafi@mib2.in / makeitbeautiful
